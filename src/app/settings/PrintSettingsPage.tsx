// =============================================================
// PRINT SETTINGS PAGE — placeholder (blank)
// =============================================================

export default function PrintSettingsPage() {
  return <div style={{ height: "100%", background: "#fff" }} />;
}
